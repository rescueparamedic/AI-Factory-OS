# AI Factory OS v2.0 Release Notes

## Release Type
Worker Runtime Engine MVP

## 핵심 추가 기능
- Worker Runtime Engine 추가
- `runtime worker-list` 명령 추가
- `runtime run` 명령 추가
- `runtime latest` 명령 추가
- `runtime list` 명령 추가
- Runtime artifact/report/run 저장
- Python/Markdown/QA/JSON Worker runtime simulation 지원

## 목적
에이전트와 워커가 실제 실행 단위처럼 결과물을 생성하고 기록하는 첫 번째 구조를 만든다.
